/// <reference types="node" />
import * as fs from 'fs';
export declare class RecordWriter {
    static create(filePath: fs.PathLike): Promise<RecordWriter>;
    writeRecord(record: Buffer): Promise<void>;
    private constructor();
    close(): Promise<void>;
    private fd_;
    private closed_;
    private lengthAndCrcBuffer_;
    private lengthAndCrc_;
    private lengthBuffer_;
}
