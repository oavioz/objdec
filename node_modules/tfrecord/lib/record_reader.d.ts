/// <reference types="node" />
import * as fs from 'fs';
export declare class RecordReader {
    static create(filePath: fs.PathLike): Promise<RecordReader>;
    readRecord(): Promise<Uint8Array | null>;
    close(): Promise<void>;
    private constructor();
    private fd_;
    private closed_;
    private lengthAndCrcBuffer_;
    private lengthAndCrc_;
    private lengthBuffer_;
    private dataBuffer_;
    private dataBufferView_;
}
