/// <reference types="snapsvg" />
import { Point2D } from "../Core/Point2D";
import { Rect } from "../Core/Rect";
import { IMovable } from "../Interface/IMovable";
import { ElementPart } from "./ElementPart";
export declare class CrossElement extends ElementPart implements IMovable {
    private hl;
    private vl;
    private center;
    readonly x: number;
    readonly y: number;
    constructor(paper: Snap.Paper, boundRect: Rect);
    boundToRect(rect: Rect): Point2D;
    move(point: IMovable): void;
    move(x: number, y: number): void;
    moveCross(p: IMovable, rect: Rect, square?: boolean, ref?: IMovable): void;
    resize(width: number, height: number): void;
    private buildUIElements;
}
